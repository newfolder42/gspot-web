// Re-export from the unified ImageUpload component.
// The account profile layout uses this import path — keep it as a shim.
export { default } from './image-upload';
export type { CropConfig, ImageUploadProps } from './image-upload';


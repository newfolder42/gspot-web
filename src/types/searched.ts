export type SearchedUser = {
  id: number;
  alias: string;
  age: number;
};

export type SearchedPost = {
  id: number;
  title: string;
  author: string;
}

export type SearchedZone = {
  id: number;
  slug: string;
  description: string;
}
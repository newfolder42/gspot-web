/**
 * Shared permission constants.
 * Centralised here so the threshold can be changed in one place.
 */

/** Minimum user level required to create a new zone. */
export const MIN_LEVEL_CREATE_ZONE = 20;
